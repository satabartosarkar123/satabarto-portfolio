"use server";

// This file previously created OpenAI ChatKit sessions.
// The portfolio now uses the Vercel AI SDK with Google Gemini via /api/chat/route.ts.
// This server action is kept as a stub to prevent import errors from any remaining references.

export async function createSession() {
  return "";
}
